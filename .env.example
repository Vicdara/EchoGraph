# Server-side provider keys (comma-separated for key pool, never commit real values)
# Required for live vision analysis. Comma-separated keys are supported.
MISTRAL_API_KEYS=
# Optional: primary microphone transcription and AI tutor.
GROQ_API_KEYS=
OPENCODE_API_KEYS=
# Optional cloud voice profile. Browser speech works without this.
MISTRAL_VOICE_ID=
# Optional tutor order: nemotron-ultra,laguna
OPENCODE_MODEL_ORDER=
